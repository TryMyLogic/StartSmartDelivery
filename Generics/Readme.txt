Generics Folder: This is a temporary folder that will contain all the generic classes implementing the generic repository pattern.
Once the generic implementation is thoroughly tested and proves to replace the original code successfully,
the original code will be deleted.
